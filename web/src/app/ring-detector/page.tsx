import RingDetector from "@/components/ring-detector";

export const metadata = {
  title: "Ring Detector | GhostQuant",
  description: "Real-time manipulation ring and coordinated wallet cluster detection",
};

export default function RingDetectorPage() {
  return <RingDetector />;
}
