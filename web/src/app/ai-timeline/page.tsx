import AITimeline from "@/components/ai-timeline";

export const metadata = {
  title: "AI Timeline | GhostQuant",
  description: "Real-time chronological intelligence event stream",
};

export default function AITimelinePage() {
  return <AITimeline />;
}
